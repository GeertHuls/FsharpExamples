// Modules are like namespace, except it contains value,
// which holds functions.

// A module at the top of the source file, applies to the
// entire file.

module A3

type Person = {name:string}
type private Thing = {a:int}
 
// --> B2
